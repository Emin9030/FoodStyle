mutation AddNewCard($name: NonEmptyString!) {
  createCard(data: {name: $name, locationCuisineTypeIds: [], dishTypeIds: [], courseTypeIds: [], dietIds: [], excludedIngredientIds: [], locationTypeIds: []}) {
    name
    id
    shareCode
  }
}
